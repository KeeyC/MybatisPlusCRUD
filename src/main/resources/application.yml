spring:
  datasource:
    url: jdbc:mysql://localhost:3306/school?characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

    logging:
      level:
        com.baomidou.example.mapper: debug


#        配置日志，可以看到sql的执行操作
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

